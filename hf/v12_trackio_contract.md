# v12 Trackio Contract

Minimum metrics to log:
- `stack_oof_acc`, `stack_oof_macro_f1`
- `dino_oof_acc`, `effnet_oof_acc`, `clip_oof_acc`
- `pseudo_count`, `pseudo_ratio`, `fallback_triggered`

If Trackio is unavailable, local JSON artifacts remain the source of truth.
