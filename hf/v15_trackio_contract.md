# v15 Trackio Contract

Minimum metrics:
- `stack_oof_acc`, `stack_oof_macro_f1`
- `dino_oof_acc`, `effnet_oof_acc`, `clip_oof_acc`
- `pseudo_count`, `pseudo_ratio`, `fallback_triggered`
- `probe_seeds`

If Trackio is unavailable, use local JSON artifacts as the source of truth.
